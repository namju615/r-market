query Post($id: Int) {
	post(id: $id) {
		member {
			member_id
			email
			name
		}
		member_id
		title
		hashtag
		price
		status
		image_url
		create_date
		update_date
		post_id
		view_count
	}
}
